DATABASE_PATH = (
    "./database/planet_data.db"  # OLD PATH WAS DATABASE_PATH = "./database/tiles.db"
)
CLOUD_COVER = 0.95
MIN_AREA_PERCENT = 0.7
TILE_STATUSES = {
    "PENDING": "pending",
    "DOWNLOADED": "downloaded",
    "DOWNLOADING": "downloading",
    "FAILED": "failed",
}
